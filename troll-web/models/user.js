const mg = require("mongoose")

const UserSchema = new mg.Schema({
    username: {
        type: String,
        required: true
    },
    email: {
        type: String,
        required: true
    },
    password: {
        type: String,
        required: true
    }
})

module.exports = mg.model("user", UserSchema)